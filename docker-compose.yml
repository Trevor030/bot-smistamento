services:
  sorting-hat-bot:
    build: .
    container_name: sorting-hat-quiz-bot
    restart: unless-stopped
    environment:
      DISCORD_TOKEN: ${DISCORD_TOKEN}
      QUIZ_CHANNEL_ID: ${QUIZ_CHANNEL_ID}
